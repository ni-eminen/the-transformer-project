
#include <vector>
#include <iomanip>
#include "Encoder.hpp"
#include "utils.hpp"
#include "Types.hpp"

int main(int argc, char *argv[])
{
    Encoder encoder = Encoder(
        double learningRate,
        int heads,
        int d_model);

    return 0;
}
